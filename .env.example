# Required: exactly 32 bytes. Example generator: openssl rand -base64 24
SESSION_SECRET=
# Empty disables registration after the first account has been created.
REGISTRATION_KEY=
# Keep false for the default http://127.0.0.1:3000 binding; use true behind HTTPS.
COOKIE_SECURE=false
