# Registration invite code: empty = registration closes after first user is created
REGISTRATION_KEY=
# Session encryption key: exactly 32 bytes, generate with: openssl rand -base64 24
SESSION_SECRET=
