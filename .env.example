# Registration invite code: empty = registration closes after first user is created
REGISTRATION_KEY=
# Session encryption key: at least 32 characters, generate with: openssl rand -hex 32
SESSION_SECRET=
